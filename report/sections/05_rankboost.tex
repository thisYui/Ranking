\section{RankBoost}
\label{sec:rankboost}

Trong các phần trước, ta đã trình bày score-based ranking và thuật toán ranking dựa trên SVM. Một hướng tiếp cận khác trong score-based ranking là sử dụng boosting. Phần này trình bày RankBoost, một thuật toán boosting được thiết kế cho bài toán ranking theo từng cặp.

Tương tự AdaBoost trong phân loại nhị phân, RankBoost kết hợp nhiều mô hình yếu, gọi là \textit{base rankers}, để tạo thành một hàm ranking mạnh hơn. Khác với AdaBoost, RankBoost duy trì phân phối trọng số trên các cặp đối tượng. Các cặp bị xếp sai ở những vòng trước sẽ được tăng trọng số, khiến thuật toán tập trung nhiều hơn vào các cặp khó ở những vòng sau.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[>=Stealth, node distance=1.25cm]
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=3.2cm, minimum height=1.05cm] (dist)
        {Phân phối trên cặp\\ \(D_t(i)\)};
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=3.2cm, minimum height=1.05cm, right=of dist] (weak)
        {Chọn base ranker\\ \(h_t\)};
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=3.2cm, minimum height=1.05cm, right=of weak] (alpha)
        {Tính trọng số\\ \(\alpha_t\)};
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=3.2cm, minimum height=1.05cm, below=1.2cm of weak] (update)
        {Cập nhật\\ \(D_{t+1}\)};
        \node[draw, rounded corners, fill=gray!20, align=center, minimum width=3.2cm, minimum height=1.05cm, below=1.2cm of alpha] (final)
        {Hàm cuối\\ \(g=\sum_t\alpha_t h_t\)};

        \draw[->, thick] (dist) -- (weak);
        \draw[->, thick] (weak) -- (alpha);
        \draw[->, thick] (alpha) -- (update);
        \draw[->, thick] (update) -- (dist);
        \draw[->, thick] (alpha) -- (final);
    \end{tikzpicture}
    \caption{Vòng lặp chính của RankBoost. Ở mỗi vòng, thuật toán chọn một base ranker tốt theo phân phối hiện tại, gán trọng số cho nó, rồi cập nhật phân phối để tập trung hơn vào các cặp khó.}
    \label{fig:section5-rankboost-loop}
\end{figure}

\subsection{Thiết lập của RankBoost}

Giả sử tập huấn luyện gồm \(m\) cặp có nhãn:
\[
    S =
    \big((x_1,x'_1,y_1),\ldots,(x_m,x'_m,y_m)\big),
    \qquad
    y_i\in\{-1,0,+1\}.
\]

Theo quy ước, \(y_i=+1\) nghĩa là \(x'_i\) được ưu tiên hơn \(x_i\), \(y_i=-1\) nghĩa là \(x_i\) được ưu tiên hơn \(x'_i\), và \(y_i=0\) nghĩa là hai đối tượng ngang nhau hoặc không có thông tin ưu tiên rõ ràng. Để đơn giản, ta giả sử các cặp có nhãn \(0\) đã được loại bỏ, tức là:
\[
    y_i\neq 0,
    \qquad i=1,\ldots,m.
\]

RankBoost sử dụng một tập các base rankers:
\[
    \mathcal{H}
    =
    \{h:\mathcal{X}\rightarrow\{0,1\}\}.
\]

Mỗi base ranker \(h_t\in\mathcal{H}\) gán cho mỗi đối tượng một giá trị trong \(\{0,1\}\). Sau \(T\) vòng, RankBoost trả về hàm điểm:
\begin{equation}
    g(x)
    =
    \sum_{t=1}^{T}
    \alpha_t h_t(x),
    \label{eq:section5-final-ranker}
\end{equation}
trong đó \(\alpha_t\geq 0\) là trọng số của base ranker \(h_t\). Ranking cuối cùng được sinh ra bằng cách sắp xếp các đối tượng theo giá trị \(g(x)\).

\subsection{Ý tưởng lặp của RankBoost}

RankBoost hoạt động theo từng vòng. Tại vòng \(t\), thuật toán duy trì một phân phối trọng số \(D_t\) trên các cặp huấn luyện. Sau đó, thuật toán chọn một base ranker \(h_t\) tốt đối với phân phối hiện tại, tính trọng số \(\alpha_t\), rồi cập nhật phân phối thành \(D_{t+1}\).

Trực giác của bước cập nhật là: các cặp được xếp đúng sẽ giảm trọng số tương đối, còn các cặp bị xếp sai sẽ tăng trọng số tương đối. Vì vậy, ở các vòng sau, thuật toán tập trung nhiều hơn vào các cặp khó.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[>=Stealth]
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=3.1cm, minimum height=1.0cm] (correct) at (0,0)
        {Cặp xếp đúng\\ trọng số giảm};
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=3.1cm, minimum height=1.0cm] (tie) at (4.0,0)
        {Cặp hoà\\ trọng số giữ nguyên};
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=3.1cm, minimum height=1.0cm] (wrong) at (8.0,0)
        {Cặp xếp sai\\ trọng số tăng};

        \node[align=center] (next) at (4,-2.0)
        {Phân phối \(D_{t+1}\) tập trung hơn\\ vào các cặp khó ở vòng sau};

        \draw[->, thick] (correct.south) -- (next.north west);
        \draw[->, thick] (tie.south) -- (next.north);
        \draw[->, thick] (wrong.south) -- (next.north east);
    \end{tikzpicture}
    \caption{Trực giác cập nhật trọng số trong RankBoost: cặp xếp sai được tăng trọng số để thuật toán chú ý hơn ở các vòng tiếp theo.}
    \label{fig:section5-weight-update-intuition}
\end{figure}

\subsection{Phân phối trọng số trên các cặp}

RankBoost duy trì một phân phối \(D_t\) trên các cặp huấn luyện:
\[
    D_t(i)\geq 0,
    \qquad
    \sum_{i=1}^{m}D_t(i)=1.
\]

Ban đầu, mọi cặp được gán trọng số bằng nhau:
\[
    D_1(i)=\frac{1}{m},
    \qquad
    i=1,\ldots,m.
\]

Tại mỗi vòng \(t\), với một cặp \((x_i,x'_i,y_i)\), base ranker \(h_t\) xếp đúng nếu:
\[
    y_i\big(h_t(x'_i)-h_t(x_i)\big)>0.
\]

Nó xếp sai nếu biểu thức trên nhỏ hơn \(0\), và không phân biệt được cặp đó nếu biểu thức bằng \(0\).

\subsection{Các đại lượng \texorpdfstring{\(\epsilon_t^+\), \(\epsilon_t^-\), \(\epsilon_t^0\)}{epsilon}}

Tại vòng \(t\), với phân phối \(D_t\), ta định nghĩa:
\begin{equation}
    \epsilon_t^{+}
    =
    \sum_{i=1}^{m}
    D_t(i)
    \mathbf{1}
    \left[
        y_i\big(h_t(x'_i)-h_t(x_i)\big)=+1
    \right],
    \label{eq:section5-epsilon-plus}
\end{equation}
\begin{equation}
    \epsilon_t^{-}
    =
    \sum_{i=1}^{m}
    D_t(i)
    \mathbf{1}
    \left[
        y_i\big(h_t(x'_i)-h_t(x_i)\big)=-1
    \right],
    \label{eq:section5-epsilon-minus}
\end{equation}
và:
\begin{equation}
    \epsilon_t^{0}
    =
    \sum_{i=1}^{m}
    D_t(i)
    \mathbf{1}
    \left[
        y_i\big(h_t(x'_i)-h_t(x_i)\big)=0
    \right].
    \label{eq:section5-epsilon-zero}
\end{equation}

Ba đại lượng này lần lượt là tổng trọng số của các cặp được xếp đúng, xếp sai và không được phân biệt bởi \(h_t\). Ta có:
\[
    \epsilon_t^{+}
    +
    \epsilon_t^{-}
    +
    \epsilon_t^{0}
    =
    1.
\]

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \draw[thick] (0,0) rectangle (10,0.9);
        \fill[gray!15] (0,0) rectangle (5.2,0.9);
        \fill[gray!35] (5.2,0) rectangle (7.2,0.9);
        \fill[gray!55] (7.2,0) rectangle (10,0.9);
        \node at (2.6,0.45) {\(\epsilon_t^+\): xếp đúng};
        \node at (6.2,0.45) {\(\epsilon_t^-\): xếp sai};
        \node at (8.6,0.45) {\(\epsilon_t^0\): hoà};
        \node[below] at (5,-0.15) {\(\epsilon_t^+ + \epsilon_t^- + \epsilon_t^0 = 1\)};
    \end{tikzpicture}
    \caption{Phân phối trọng số \(D_t\) được chia thành ba nhóm: tổng trọng số của cặp xếp đúng, cặp xếp sai và cặp hoà.}
    \label{fig:section5-epsilon-decomposition}
\end{figure}

RankBoost chọn base ranker sao cho \(\epsilon_t^{-}-\epsilon_t^{+}\) nhỏ nhất, tức là ưu tiên base ranker có trọng số xếp đúng lớn và trọng số xếp sai nhỏ. Edge của base ranker được định nghĩa là:
\begin{equation}
    \gamma_t
    =
    \frac{\epsilon_t^{+}-\epsilon_t^{-}}{2}.
    \label{eq:section5-edge}
\end{equation}

Nếu \(\gamma_t>0\), base ranker \(h_t\) xếp đúng nhiều hơn xếp sai theo phân phối hiện tại. Đây là điều kiện yếu giúp boosting cải thiện dần qua các vòng.

\subsection{Trọng số của base ranker}

Sau khi chọn được \(h_t\), RankBoost gán cho nó trọng số:
\begin{equation}
    \alpha_t
    =
    \frac{1}{2}
    \log
    \frac{\epsilon_t^{+}}{\epsilon_t^{-}}.
    \label{eq:section5-alpha}
\end{equation}

Nếu \(\epsilon_t^{+}>\epsilon_t^{-}\), thì \(\alpha_t>0\). Base ranker càng tốt, tức là \(\epsilon_t^{+}\) càng lớn so với \(\epsilon_t^{-}\), thì \(\alpha_t\) càng lớn. Vì vậy, các base rankers mạnh hơn sẽ có ảnh hưởng lớn hơn trong hàm điểm cuối cùng \(g\).

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[>=Stealth]
        \draw[->, thick] (0,0) -- (8,0) node[right] {\(\epsilon_t^+/\epsilon_t^-\)};
        \draw[->, thick] (0,-1.5) -- (0,2.0) node[above] {\(\alpha_t\)};
        \draw[domain=0.35:7.5, smooth, variable=\x, thick]
            plot ({\x},{0.7213*ln(\x)});
        \draw[dashed] (1,-1.5) -- (1,2.0);
        \node[below] at (1,0) {\(1\)};
        \node[right] at (5.2,1.25) {\(\alpha_t=\frac12\log(\epsilon_t^+/\epsilon_t^-)\)};
        \node[align=center] at (2.2,-1.0) {\(\epsilon_t^+>\epsilon_t^-\)\\ \(\Rightarrow \alpha_t>0\)};
    \end{tikzpicture}
    \caption{Trọng số \(\alpha_t\) tăng khi base ranker xếp đúng nhiều hơn xếp sai theo phân phối hiện tại.}
    \label{fig:section5-alpha-ratio}
\end{figure}

\subsection{Cập nhật phân phối trọng số}

Sau khi chọn \(h_t\) và \(\alpha_t\), RankBoost cập nhật phân phối trọng số trên các cặp:
\begin{equation}
    D_{t+1}(i)
    =
    \frac{
        D_t(i)
        \exp
        \left(
            -\alpha_t
            y_i
            \big(h_t(x'_i)-h_t(x_i)\big)
        \right)
    }{
        Z_t
    },
    \label{eq:section5-distribution-update}
\end{equation}
trong đó \(Z_t\) là hệ số chuẩn hoá:
\begin{equation}
    Z_t
    =
    \sum_{i=1}^{m}
    D_t(i)
    \exp
    \left(
        -\alpha_t
        y_i
        \big(h_t(x'_i)-h_t(x_i)\big)
    \right).
    \label{eq:section5-normalization-factor}
\end{equation}

Hệ số \(Z_t\) đảm bảo \(D_{t+1}\) vẫn là một phân phối. Nếu cặp thứ \(i\) được xếp đúng, trọng số của nó được nhân với \(e^{-\alpha_t}\). Nếu cặp đó bị xếp sai, trọng số được nhân với \(e^{\alpha_t}\). Nếu cặp bị hoà, hệ số nhân là \(1\).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{figures/sec5_weight_update_effect.pdf}
    \caption{Ảnh hưởng của \(\alpha\) lên hệ số nhân trọng số trong RankBoost. Khi \(\alpha\) tăng, cặp xếp đúng được giảm trọng số theo \(e^{-\alpha}\), cặp xếp sai được tăng trọng số theo \(e^\alpha\), còn cặp hoà giữ nguyên hệ số \(1\).}
    \label{fig:section5-alpha-weight-update}
\end{figure}

Hình~\ref{fig:section5-alpha-weight-update} cho thấy rõ vai trò của \(\alpha_t\). Khi base ranker tốt hơn, \(\alpha_t\) lớn hơn, làm cho các cặp xếp sai được phóng đại trọng số mạnh hơn ở vòng sau. Ngược lại, các cặp đã được xếp đúng bị giảm trọng số, vì chúng không còn là trọng tâm chính của thuật toán.

Vì \(\alpha_t>0\), các cặp bị xếp sai được tăng trọng số tương đối, còn các cặp được xếp đúng bị giảm trọng số tương đối. Đây là cơ chế giúp RankBoost tập trung vào các cặp khó hơn qua từng vòng.

\subsection{Thuật toán RankBoost}

Thuật toán RankBoost có thể tóm tắt như sau.

\begin{enumerate}[label=\textbf{Bước \arabic*.}, leftmargin=*]
    \item Khởi tạo phân phối đều:
    \[
        D_1(i)=\frac{1}{m},
        \qquad
        i=1,\ldots,m.
    \]

    \item Với mỗi vòng \(t=1,\ldots,T\), chọn base ranker:
    \[
        h_t
        \in
        \arg\min_{h\in\mathcal{H}}
        \left(
            \epsilon_t^{-}(h)-\epsilon_t^{+}(h)
        \right).
    \]

    \item Tính trọng số:
    \[
        \alpha_t
        =
        \frac{1}{2}
        \log
        \frac{\epsilon_t^{+}}{\epsilon_t^{-}}.
    \]

    \item Cập nhật phân phối theo công thức~\eqref{eq:section5-distribution-update}.

    \item Sau \(T\) vòng, trả về:
    \[
        g(x)
        =
        \sum_{t=1}^{T}
        \alpha_t h_t(x).
    \]
\end{enumerate}

Hàm \(g\) sau cùng được dùng để xếp hạng các đối tượng: đối tượng nào có \(g(x)\) lớn hơn sẽ được xếp cao hơn.

\subsection{Biểu diễn phân phối sau nhiều vòng}

Để phân tích RankBoost, ta cần biểu diễn \(D_{t+1}\) theo tổ hợp các base rankers đã chọn. Gọi:
\[
    g_t(x)
    =
    \sum_{s=1}^{t}
    \alpha_s h_s(x).
\]

Khi đó, bằng cách khai triển đệ quy công thức cập nhật, ta có:
\begin{equation}
    D_{t+1}(i)
    =
    \frac{
        \exp
        \left(
            -y_i
            \big(g_t(x'_i)-g_t(x_i)\big)
        \right)
    }{
        m
        \prod_{s=1}^{t}Z_s
    }.
    \label{eq:section5-distribution-closed-form}
\end{equation}

Công thức này cho thấy trọng số của một cặp sau nhiều vòng phụ thuộc vào margin tích luỹ của hàm \(g_t\) trên cặp đó. Nếu cặp có margin lớn và đúng chiều, trọng số giảm; nếu cặp bị xếp sai hoặc margin nhỏ, trọng số vẫn lớn.

\subsection{Chặn lỗi thực nghiệm của RankBoost}

Sau \(T\) vòng, RankBoost trả về:
\[
    g(x)=g_T(x)=\sum_{t=1}^{T}\alpha_t h_t(x).
\]

Lỗi thực nghiệm của \(g\) là:
\begin{equation}
    \widehat{R}(g)
    =
    \frac{1}{m}
    \sum_{i=1}^{m}
    \mathbf{1}
    \left[
        y_i
        \big(g(x'_i)-g(x_i)\big)
        \leq 0
    \right].
    \label{eq:section5-rankboost-empirical-error}
\end{equation}

Ta có chặn lỗi thực nghiệm sau.

\paragraph{Định lý 5.1.}
Lỗi thực nghiệm của hàm \(g\) trả về bởi RankBoost thoả:
\begin{equation}
    \widehat{R}(g)
    \leq
    \exp
    \left(
        -2
        \sum_{t=1}^{T}
        \left(
            \frac{\epsilon_t^{+}-\epsilon_t^{-}}{2}
        \right)^2
    \right).
    \label{eq:section5-rankboost-empirical-bound}
\end{equation}

Nếu tồn tại \(\gamma>0\) sao cho với mọi \(t=1,\ldots,T\),
\[
    \gamma
    \leq
    \frac{\epsilon_t^{+}-\epsilon_t^{-}}{2},
\]
thì:
\begin{equation}
    \widehat{R}(g)
    \leq
    \exp(-2\gamma^2T).
    \label{eq:section5-rankboost-exponential-bound}
\end{equation}

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[>=Stealth]
        \draw[->, thick] (0,0) -- (8.5,0) node[right] {số vòng \(T\)};
        \draw[->, thick] (0,0) -- (0,4.2) node[above] {\(\widehat{R}(g)\)};
        \draw[domain=0:8, smooth, variable=\x, thick]
            plot ({\x},{3.6*exp(-0.32*\x)});
        \node[right] at (4.5,1.0) {\(\widehat{R}(g)\leq e^{-2\gamma^2T}\)};
        \draw[dashed] (0,3.6) -- (8,0.28);
        \node[align=center] at (5.6,3.2) {edge dương qua nhiều vòng\\ \(\Rightarrow\) lỗi giảm theo hàm mũ};
    \end{tikzpicture}
    \caption{Nếu mỗi vòng tìm được base ranker có edge dương, chặn trên của lỗi thực nghiệm giảm theo hàm mũ theo số vòng boosting.}
    \label{fig:section5-exponential-error-bound}
\end{figure}

Định lý cho thấy nếu mỗi vòng boosting tìm được một base ranker có edge dương ít nhất \(\gamma\), lỗi thực nghiệm của RankBoost giảm theo hàm mũ theo số vòng \(T\). Đây là tính chất tương tự AdaBoost trong bài toán phân loại nhị phân.

\subsection{Chứng minh Định lý 5.1}

Đặt margin tích luỹ của hàm cuối \(g\) trên cặp thứ \(i\) là
\[
    u_i
    =
    y_i
    \big(g(x'_i)-g(x_i)\big).
\]
Nếu cặp thứ \(i\) bị xếp sai hoặc hoà thì \(u_i\leq 0\). Sử dụng bất đẳng thức
\[
    \mathbf{1}[u\leq 0]\leq \exp(-u),
    \qquad
    \forall u\in\mathbb{R},
\]
ta có:
\[
    \widehat{R}(g)
    \leq
    \frac{1}{m}
    \sum_{i=1}^{m}
    \exp
    \left(
        -y_i
        \big(g(x'_i)-g(x_i)\big)
    \right).
\]

Ta chứng minh liên hệ giữa tổng mũ ở vế phải và các hệ số chuẩn hoá \(Z_t\). Từ quy tắc cập nhật:
\[
    D_{t+1}(i)
    =
    \frac{
        D_t(i)
        \exp
        \left(
            -\alpha_t
            y_i
            \big(h_t(x'_i)-h_t(x_i)\big)
        \right)
    }{
        Z_t
    },
\]
và \(D_1(i)=1/m\), khai triển đệ quy cho \(t=1,\ldots,T\) cho ta:
\[
    D_{T+1}(i)
    =
    \frac{
        \exp
        \left(
            -y_i
            \sum_{t=1}^{T}
            \alpha_t
            \big(h_t(x'_i)-h_t(x_i)\big)
        \right)
    }{
        m\prod_{t=1}^{T} Z_t
    }.
\]
Vì \(g(x)=\sum_{t=1}^{T}\alpha_t h_t(x)\), công thức trên trở thành:
\[
    D_{T+1}(i)
    =
    \frac{
        \exp
        \left(
            -y_i
            \big(g(x'_i)-g(x_i)\big)
        \right)
    }{
        m\prod_{t=1}^{T} Z_t
    }.
\]
Lấy tổng theo \(i\) và dùng \(\sum_i D_{T+1}(i)=1\), ta suy ra:
\[
    \frac{1}{m}
    \sum_{i=1}^{m}
    \exp
    \left(
        -y_i
        \big(g(x'_i)-g(x_i)\big)
    \right)
    =
    \prod_{t=1}^{T}Z_t.
\]
Do đó:
\[
    \widehat{R}(g)
    \leq
    \prod_{t=1}^{T}Z_t.
\]

Tiếp theo, ta chặn từng \(Z_t\). Tại vòng \(t\), đặt
\[
    r_i^{(t)}
    =
    y_i
    \big(h_t(x'_i)-h_t(x_i)\big).
\]
Vì base ranker nhận giá trị trong \(\{0,1\}\), ta có \(r_i^{(t)}\in\{-1,0,+1\}\). Chia các cặp thành ba nhóm đúng, sai và hoà theo giá trị của \(r_i^{(t)}\), ta được:
\[
    Z_t
    =
    \epsilon_t^{+}e^{-\alpha_t}
    +
    \epsilon_t^{-}e^{\alpha_t}
    +
    \epsilon_t^{0}.
\]

Với lựa chọn
\[
    \alpha_t
    =
    \frac{1}{2}
    \log
    \frac{\epsilon_t^{+}}{\epsilon_t^{-}},
\]
ta thu được:
\[
    Z_t
    =
    2\sqrt{\epsilon_t^{+}\epsilon_t^{-}}
    +
    \epsilon_t^{0}.
\]

Ta đặt
\[
    a_t=\epsilon_t^{+}+\epsilon_t^{-},
    \qquad
    d_t=\epsilon_t^{+}-\epsilon_t^{-}.
\]
Khi đó \(\epsilon_t^0=1-a_t\) và
\[
    4\epsilon_t^{+}\epsilon_t^{-}
    =
    a_t^2-d_t^2.
\]
Vì vậy:
\[
\begin{aligned}
    Z_t
    &=
    \epsilon_t^{0}
    +
    2\sqrt{\epsilon_t^{+}\epsilon_t^{-}}\\
    &=
    1-a_t+\sqrt{a_t^2-d_t^2}\\
    &=
    1-a_t+a_t\sqrt{1-\frac{d_t^2}{a_t^2}}.
\end{aligned}
\]
Với \(0\leq x\leq 1\), ta có \(\sqrt{1-x}\leq 1-x/2\). Áp dụng cho \(x=d_t^2/a_t^2\), suy ra:
\[
\begin{aligned}
    Z_t
    &\leq
    1-a_t
    +
    a_t
    \left(
        1-\frac{d_t^2}{2a_t^2}
    \right)\\
    &=
    1-\frac{d_t^2}{2a_t}
    \leq
    1-\frac{d_t^2}{2}.
\end{aligned}
\]
Bất đẳng thức cuối dùng \(a_t\leq 1\). Theo định nghĩa edge,
\[
    \gamma_t
    =
    \frac{\epsilon_t^{+}-\epsilon_t^{-}}{2}
    =
    \frac{d_t}{2}.
\]
Do đó:
\[
    Z_t
    \leq
    1-2\gamma_t^2.
\]
Cuối cùng, vì \(1-x\leq e^{-x}\) với mọi \(x\), ta có:
\[
    Z_t
    \leq
    \exp
    \left(
        -2
        \gamma_t^2
    \right).
\]
Trường hợp \(a_t=0\) tương ứng với mọi cặp đều bị hoà, khi đó \(\gamma_t=0\), \(Z_t=1\), và chặn trên vẫn đúng. Trường hợp \(\epsilon_t^{-}=0\) có thể hiểu theo giới hạn \(\alpha_t\to+\infty\); chặn vẫn thu được bằng lập luận giới hạn hoặc bằng cách thêm một lượng làm trơn rất nhỏ rồi cho lượng đó tiến về \(0\).

Thay chặn này vào \(\widehat{R}(g)\leq\prod_{t=1}^{T}Z_t\), ta thu được:
\[
    \widehat{R}(g)
    \leq
    \exp
    \left(
        -2
        \sum_{t=1}^{T}
        \gamma_t^2
    \right).
\]
Vì \(\gamma_t=(\epsilon_t^{+}-\epsilon_t^{-})/2\), bất đẳng thức này chính là:
\[
    \widehat{R}(g)
    \leq
    \exp
    \left(
        -2
        \sum_{t=1}^{T}
        \left(
            \frac{\epsilon_t^{+}-\epsilon_t^{-}}{2}
        \right)^2
    \right).
\]

Nếu mỗi edge đều không nhỏ hơn \(\gamma\), thì tổng trong mũ không nhỏ hơn \(T\gamma^2\), và do đó:
\[
    \widehat{R}(g)
    \leq
    \exp(-2\gamma^2T).
\]

\subsection{[MỞ RỘNG] Vì sao \texorpdfstring{\(\alpha_t\)}{alpha t} tối thiểu hoá \texorpdfstring{\(Z_t\)}{Zt}}

Hệ số \(\alpha_t\) có thể được giải thích bằng cách xem \(Z_t\) như một hàm của \(\alpha\):
\[
    Z_t(\alpha)
    =
    \epsilon_t^{+}e^{-\alpha}
    +
    \epsilon_t^{-}e^{\alpha}
    +
    \epsilon_t^{0}.
\]

Lấy đạo hàm theo \(\alpha\):
\[
    \frac{dZ_t(\alpha)}{d\alpha}
    =
    -\epsilon_t^{+}e^{-\alpha}
    +
    \epsilon_t^{-}e^{\alpha}.
\]

Cho đạo hàm bằng \(0\), ta được:
\[
    e^{2\alpha}
    =
    \frac{\epsilon_t^{+}}{\epsilon_t^{-}}.
\]

Do đó:
\[
    \alpha
    =
    \frac{1}{2}
    \log
    \frac{\epsilon_t^{+}}{\epsilon_t^{-}}.
\]

Vì vậy, công thức chọn \(\alpha_t\) trong RankBoost được chọn để tối thiểu hoá hệ số chuẩn hoá \(Z_t\), mà tích các \(Z_t\) lại là một chặn trên của lỗi thực nghiệm.

\subsection{RankBoost và coordinate descent}

RankBoost cũng có thể được hiểu như việc áp dụng coordinate descent lên một hàm mục tiêu lồi và khả vi. Xét hàm:
\begin{equation}
    F(\alpha)
    =
    \sum_{i=1}^{m}
    \exp
    \left(
        -y_i
        \left[
            g(x'_i)-g(x_i)
        \right]
    \right),
    \label{eq:section5-coordinate-descent-objective}
\end{equation}
trong đó:
\[
    g(x)
    =
    \sum_{t=1}^{T}
    \alpha_t h_t(x).
\]

Hàm \(F(\alpha)\) là một chặn lồi của lỗi pairwise misranking dạng chỉ thị. Ở mỗi vòng, RankBoost chọn base ranker \(h_t\) tương ứng với một hướng toạ độ làm giảm nhanh nhất hàm \(F\). Sau đó, hệ số \(\alpha_t\) được chọn để tối thiểu hoá \(F\) theo hướng đó. Vì vậy, RankBoost có thể được xem là coordinate descent trong không gian các tổ hợp tuyến tính của base rankers.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[>=Stealth, node distance=1.2cm]
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=3.4cm, minimum height=1cm] (obj)
        {Hàm mục tiêu\\ \(F(\alpha)\)};
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=3.4cm, minimum height=1cm, right=of obj] (coord)
        {Chọn toạ độ\\ \(h_t\)};
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=3.4cm, minimum height=1cm, right=of coord] (step)
        {Tối ưu bước\\ \(\alpha_t\)};
        \draw[->, thick] (obj) -- (coord);
        \draw[->, thick] (coord) -- (step);
        \draw[->, thick, bend left=35] (step.north) to node[above] {lặp lại} (obj.north);
    \end{tikzpicture}
    \caption{Góc nhìn coordinate descent: RankBoost chọn một hướng \(h_t\) rồi tối ưu hệ số \(\alpha_t\) theo hướng đó.}
    \label{fig:section5-coordinate-descent-view}
\end{figure}

\subsection{Ví dụ minh hoạ}

Xét ba tài liệu \(d_1,d_2,d_3\) với thứ tự đúng:
\[
    d_1 \succ d_2 \succ d_3.
\]

Ta xét ba cặp:
\[
    (d_2,d_1,+1),
    \qquad
    (d_3,d_1,+1),
    \qquad
    (d_3,d_2,+1).
\]

Ban đầu, mỗi cặp có trọng số \(1/3\). Giả sử base ranker \(h_1\) cho điểm:
\[
    h_1(d_1)=1,
    \qquad
    h_1(d_2)=1,
    \qquad
    h_1(d_3)=0.
\]

Khi đó, cặp \((d_2,d_1)\) bị hoà vì \(h_1(d_1)-h_1(d_2)=0\), còn hai cặp \((d_3,d_1)\) và \((d_3,d_2)\) được xếp đúng. Vì vậy:
\[
    \epsilon_1^{+}=\frac{2}{3},
    \qquad
    \epsilon_1^{-}=0,
    \qquad
    \epsilon_1^{0}=\frac{1}{3}.
\]

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \matrix[matrix of nodes,
                nodes={draw, minimum width=3.0cm, minimum height=0.8cm, align=center},
                column sep=-\pgflinewidth,
                row sep=-\pgflinewidth] (m) {
            Cặp & Hiệu \(h_1(x')-h_1(x)\) & Nhóm \\
            \((d_2,d_1,+1)\) & \(1-1=0\) & hoà \\
            \((d_3,d_1,+1)\) & \(1-0=1\) & đúng \\
            \((d_3,d_2,+1)\) & \(1-0=1\) & đúng \\
        };
    \end{tikzpicture}
    \caption{Ví dụ phân loại ba cặp thành nhóm đúng, sai hoặc hoà theo base ranker \(h_1\).}
    \label{fig:section5-rankboost-example-table}
\end{figure}

Ví dụ này cho thấy vai trò của ba nhóm cặp: xếp đúng, xếp sai và hoà điểm. Trong trường hợp \(\epsilon_t^{-}=0\), công thức \(\alpha_t=\frac{1}{2}\log(\epsilon_t^{+}/\epsilon_t^{-})\) không xác định hữu hạn; thực tế có thể xử lý bằng làm trơn nhỏ hoặc dừng sớm nếu không còn cặp bị xếp sai.

\subsection{[MỞ RỘNG] Phân tích độ phức tạp của RankBoost}

Giả sử tập huấn luyện có \(m\) cặp và thuật toán chạy trong \(T\) vòng. Nếu bỏ qua chi phí tìm base ranker, việc tính các đại lượng \(\epsilon_t^{+}\), \(\epsilon_t^{-}\), \(\epsilon_t^{0}\) cần duyệt qua \(m\) cặp, nên có chi phí \(O(m)\). Việc cập nhật phân phối \(D_{t+1}\) cũng có chi phí \(O(m)\).

Do đó, chi phí mỗi vòng là:
\[
    O(m),
\]
và sau \(T\) vòng, tổng chi phí là:
\[
    O(Tm).
\]

Nếu \(m\) được tạo từ tất cả các cặp của \(n\) đối tượng, thì:
\[
    m=
    \frac{n(n-1)}{2}
    =
    O(n^2).
\]

Khi đó, chi phí tổng quát có thể trở thành \(O(Tn^2)\), còn bộ nhớ để lưu phân phối trên các cặp là \(O(n^2)\). Điều này cho thấy RankBoost có thể gặp khó khăn khi số lượng cặp rất lớn, và là động lực để xét các phiên bản hiệu quả hơn trong bipartite ranking.

\begin{table}[H]
    \centering
    \begin{tabular}{lll}
        \toprule
        \textbf{Thao tác} & \textbf{Chi phí} & \textbf{Ghi chú} \\
        \midrule
        Tính \(\epsilon_t^+,\epsilon_t^-,\epsilon_t^0\) & \(O(m)\) & Duyệt qua toàn bộ cặp \\
        Cập nhật \(D_{t+1}\) & \(O(m)\) & Chuẩn hoá bằng \(Z_t\) \\
        Một vòng boosting & \(O(m)\) & Chưa tính chi phí tìm \(h_t\) \\
        \(T\) vòng boosting & \(O(Tm)\) & Nếu \(m=O(n^2)\), chi phí là \(O(Tn^2)\) \\
        \bottomrule
    \end{tabular}
    \caption{Tóm tắt độ phức tạp của RankBoost trong thiết lập pairwise tổng quát.}
    \label{tab:section5-rankboost-complexity}
\end{table}

\subsection{Liên hệ với AdaBoost}

RankBoost có quan hệ chặt chẽ với AdaBoost. Cả hai thuật toán đều duy trì một phân phối trọng số, chọn một weak learner tại mỗi vòng, gán trọng số cho weak learner đó và cập nhật phân phối để tập trung vào các mẫu khó.

Điểm khác biệt chính là AdaBoost duy trì trọng số trên từng điểm dữ liệu \((x_i,y_i)\), còn RankBoost duy trì trọng số trên từng cặp \((x_i,x'_i,y_i)\). Vì vậy, AdaBoost cố gắng giảm lỗi phân loại từng điểm, còn RankBoost cố gắng giảm lỗi xếp hạng theo từng cặp.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[>=Stealth, node distance=1.4cm]
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=4cm, minimum height=1.1cm] (ada)
        {AdaBoost\\ trọng số trên điểm\\ \((x_i,y_i)\)};
        \node[draw, rounded corners, fill=gray!10, align=center, minimum width=4cm, minimum height=1.1cm, right=2cm of ada] (rank)
        {RankBoost\\ trọng số trên cặp\\ \((x_i,x'_i,y_i)\)};
        \node[align=center, below=1.0cm of ada] (adaobj)
        {giảm lỗi phân loại};
        \node[align=center, below=1.0cm of rank] (rankobj)
        {giảm lỗi xếp hạng theo cặp};
        \draw[->, thick] (ada) -- (adaobj);
        \draw[->, thick] (rank) -- (rankobj);
    \end{tikzpicture}
    \caption{AdaBoost và RankBoost có cùng tinh thần boosting, nhưng khác nhau ở đối tượng được gán trọng số.}
    \label{fig:section5-adaboost-rankboost}
\end{figure}

\subsection{Tóm tắt}

RankBoost là một thuật toán boosting cho bài toán ranking theo từng cặp. Thuật toán duy trì phân phối trọng số trên các cặp huấn luyện, chọn base ranker có khả năng xếp đúng các cặp có trọng số lớn, sau đó cập nhật phân phối để tập trung vào các cặp bị xếp sai. Hàm ranking cuối cùng là tổ hợp tuyến tính của các base rankers.

Các hình trong phần này nhấn mạnh ba ý chính. Thứ nhất, RankBoost hoạt động theo vòng lặp chọn base ranker, tính \(\alpha_t\), rồi cập nhật phân phối. Thứ hai, hệ số \(\alpha_t\) điều khiển mức tăng hoặc giảm trọng số của các cặp sau mỗi vòng. Thứ ba, nếu mỗi vòng có edge dương, chặn trên của lỗi thực nghiệm giảm theo hàm mũ theo số vòng boosting.

Kết quả lý thuyết quan trọng của RankBoost là lỗi thực nghiệm giảm theo hàm mũ nếu tại mỗi vòng tồn tại một base ranker có edge dương. Ngoài ra, RankBoost có thể được hiểu như coordinate descent trên một hàm mục tiêu lồi dạng exponential loss. Phân tích này tạo nền tảng cho phần tiếp theo, nơi ta xét trường hợp đặc biệt nhưng rất quan trọng của ranking: bipartite ranking.
